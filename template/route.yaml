apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: pipeline-hello-world
  name: pipeline-hello-world
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: pipeline-hello-world
    weight: 100
